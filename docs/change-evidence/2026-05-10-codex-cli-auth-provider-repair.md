# Codex CLI Auth Provider Repair Evidence

- rule_id: `R1/R6/R8`
- risk_level: `low`
- current_landing: live `C:\Users\sciman\.codex`
- target_home: `D:\CODE\governed-ai-coding-runtime`
- issue: direct `codex` startup reported login-mode mismatch between ChatGPT and API-key auth.
- root_cause: live `config.toml` had drifted provider/login semantics; `auth.json` had also been removed during the failed startup/logout path.
- change: restored live auth projection through `scripts\Optimize-CodexLocal.ps1 -Apply` and made `Optimize-CodexLocal.ps1` persist top-level `model_provider = "openai"` plus explicit `profiles.shared-chatgpt.model_provider = "openai"`.
- commands:
  - `codex login status`
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Optimize-CodexLocal.ps1 -Apply`
  - `codex exec --json --ephemeral "只输出 OK"`
  - `python -m unittest tests.runtime.test_codex_shared_launcher`
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\build-runtime.ps1`
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\verify-repo.ps1 -Check Runtime`
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\verify-repo.ps1 -Check Contract`
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\doctor-runtime.ps1`
- key_output:
  - `codex login status`: `Logged in using an API key`
  - optimizer interop `after.status`: `pass`
  - optimizer active provider bucket: `openai`
  - `codex exec`: emitted final agent message `OK`
  - `test_codex_shared_launcher`: `Ran 8 tests ... OK`
  - build: `OK python-bytecode`, `OK python-import`
  - Runtime: `Completed 106 test files ... failures=0`
  - Contract: all listed contract checks `OK`, including `agent-rule-sync`, `pre-change-review`, `functional-effectiveness`
  - doctor: `OK` for runtime path, dependency baseline, policy, hook, gate command, and status checks; existing `WARN codex-capability-degraded` remains native attach capability evidence only.
- compatibility: custom relay remains reachable through the shared `openai` provider bucket with `openai_base_url = "http://35.213.82.91:8003/v1"`; ChatGPT profile no longer inherits a custom provider.
- rollback:
  - live config backup: `C:\Users\sciman\.codex\config-backups\config-20260510-001027.toml`
  - interop backup: `C:\Users\sciman\.codex\backups\config.toml.20260510_001027_cockpit_provider.bak`
  - git rollback for source change: revert `scripts\Optimize-CodexLocal.ps1` and this evidence file.
