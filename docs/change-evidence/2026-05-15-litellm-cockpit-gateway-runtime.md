# LiteLLM / Cockpit Gateway Runtime Evidence - 2026-05-15

## Scope
- Rule IDs: `LocalCodexCockpit`, `CCHS-005A`, `CCHS-005B`, `CCHS-005C`, `CCHS-005D`, `CCHS-005F`.
- Risk: medium local runtime/config change.
- pre_change_review: completed for local runtime script, Codex config profile, Cockpit saved provider projection, and plan/runbook updates.
- Current landing: `scripts/Manage-LiteLLMGateway.ps1`, `docs/runbooks/litellm-cockpit-gateway.md`, `tests/runtime/test_litellm_gateway_contract.py`, local `.runtime/litellm/`, and additive `~/.codex/config.toml` provider/profile blocks.
- Target destination: opt-in `Codex -> LiteLLM -> Cockpit API service` gateway lane with Cockpit remaining the account owner/account-pool router and LiteLLM remaining the client-facing gateway.
- Rollback: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Manage-LiteLLMGateway.ps1 -Action Rollback`, then restore `~/.codex/config.toml` from the listed backup if saved Cockpit API provider projection must be removed.

## Drift Review Inputs
- control_repo_manifest_and_rule_sources: inspected active project `AGENTS.md` contract and followed the `LocalCodexCockpit` boundary requiring the repair runbook plus `CodexProjectionSmoke` before local interop changes.
- user_level_deployed_rule_files: no user-level rule file was modified; `~/.codex/config.toml` was changed only by additive provider/profile blocks and backed up under `~/.codex/config-backups/`.
- target_repo_deployed_rule_files: no target repo rule files were synchronized or edited by this lane.
- target_repo_gate_scripts_and_ci: this lane adds `scripts/Manage-LiteLLMGateway.ps1` and contract tests only; existing gate order remains `build -> Runtime -> Contract -> hotspot`.
- target_repo_repo_profile: no `.governed-ai` target profile or repo profile was changed.
- target_repo_readme_and_operator_docs: operator-facing documentation is in `docs/runbooks/litellm-cockpit-gateway.md` and `docs/plans/codex-cli-continuity-and-hot-switch-plan.md`; README surface was not expanded.
- current_official_tool_loading_docs: Codex custom-provider config uses a non-built-in provider id, `wire_api="responses"`, `env_key`, `requires_openai_auth=false`, and `supports_websockets=false`; it does not override reserved built-in provider id `openai`.
- drift-integration decision: keep old CCHS segmented-runner code as fallback; do not delete it after the LiteLLM lane live smoke because it remains the rollback/continuity path when gateway mode is disabled.

## Commands And Evidence
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Manage-LiteLLMGateway.ps1 -Action All`
  - Result: LiteLLM installed under `.runtime/litellm/venv`.
  - Key output: `LiteLLM: Current Version = 1.84.0`.
  - Key output: LiteLLM listener present on `127.0.0.1:4000`.
  - Key output: `/v1/models` returned `cockpit-current` with `secret_redacted=true`.
  - Key output: Codex profile `litellm-gateway` written without changing the active profile.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Manage-LiteLLMGateway.ps1 -Action CockpitStatus`
  - Initial result: `enabled=false`, `listener_count=0`, `safe_for_upstream=false`.
  - Final result: `enabled=true`, `account_count=3`, listener addresses include `0.0.0.0`, `firewall_profile_default_block=true`, `safe_for_upstream=true`.
  - Compatibility judgment: Cockpit local access is usable as a LiteLLM upstream because inbound LAN access is constrained by Windows Firewall profile policy `BlockInbound,AllowOutbound`.
- `codex --profile litellm-gateway debug models`
  - Result: exit code `0`.
  - Compatibility judgment: Codex can parse the additive profile and custom provider without overriding built-in `openai`.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\operator.ps1 -Action CodexLaunchBindingRepair`
  - Result: `codex_instances.json` now follows the current account with no fixed `bindAccountId`.
  - Backup: `C:\Users\sciman\.antigravity_cockpit\backups\codex_instances.json.20260515_213051_cockpit-instance-follow-current.bak`.
- Additive saved Cockpit API provider projection
  - Result: `~/.codex/config.toml` now contains no-secret metadata for `model_providers.cmp_1778165666417_1`.
  - Backup: `C:\Users\sciman\.codex\config-backups\config.toml.cockpit-saved-provider-20260515-213405.bak`.
- Cockpit local access key hygiene
  - Result: local access key rotated without printing the new key; LiteLLM secrets were re-rendered and LiteLLM was restarted.
  - Backup: `C:\Users\sciman\.antigravity_cockpit\backups\codex_local_access.json.20260515_213455.litellm-key-rotate.bak`.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Manage-LiteLLMGateway.ps1 -Action PrepareCockpitUpstream`
  - Result: `enabled=true`, `port=2876`, `account_count=3`, `firewall_profile_default_block=true`, `restart_required=true`.
  - Backup: `C:\Users\sciman\.antigravity_cockpit\backups\codex_local_access.json.20260515-222907.enable-litellm-upstream.bak`.
  - Compatibility judgment: account pool uses non-free OAuth accounts only; API-key-shaped accounts are excluded and `restrictFreeAccounts=true`.
- Cockpit Tools restart for local access runtime load
  - Result: `cockpit-tools.exe` restarted from PID `21324` to PID `24036`.
  - Boundary: Codex App and `codex` processes were not restarted, stopped, killed, or auto-launched.
- Direct Cockpit API service smoke
  - Command shape: `GET http://127.0.0.1:2876/v1/models` with local access key redacted.
  - Result: `modelCount=9`, `contains_gpt_5_4=true`, `secret_redacted=true`.
- LiteLLM live upstream smoke
  - Command shape: `POST http://127.0.0.1:4000/v1/chat/completions` with model `cockpit-current`.
  - Result: `status=ok`, `content=ok`, `model=cockpit-current`, `secret_redacted=true`.
  - Debug note: LiteLLM `master_key` must be rendered under `general_settings` for version `1.84.0`; placing it elsewhere causes the proxy to treat the key as a virtual key and fail with `No connected db`.
- Codex CLI end-to-end smoke
  - Command: `codex exec --profile litellm-gateway --model cockpit-current --cd D:\CODE\governed-ai-coding-runtime --skip-git-repo-check -o <temp> "Reply exactly: ok"`.
  - Result: final message `ok`.
  - Key output: `provider: litellm_gateway`, `model: cockpit-current`.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\operator.ps1 -Action CodexProjectionSmoke`
  - Result: `status=pass`.
  - Key checks: history provider bucket `openai`, current Cockpit OAuth projection, saved API provider projectability, guard absence, and follow-current launch binding all passed.
- `python -m unittest tests.runtime.test_litellm_gateway_contract -v`
  - Result: 4 tests passed.
- `python -m unittest tests.runtime.test_shell_risk_contract tests.runtime.test_codex_cockpit_policy_contract tests.runtime.test_litellm_gateway_contract -v`
  - Result: 14 tests passed.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\operator.ps1 -Action DailyAll -Mode quick`
  - Result: refreshed six target-run evidence files with stamp `20260515215447`; five targets passed on the first run and `self-runtime` initially failed because contract executed before the refreshed evidence posture was available.
  - Follow-up: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\runtime-flow-preset.ps1 -Target self-runtime -FlowMode daily -Mode quick -Json -ExportTargetRepoRuns`
  - Follow-up result: `self-runtime-daily-20260515220053.json` passed.
- `python scripts\host-feedback-summary.py --assert-minimum`
  - Result: minimum surface passed; target-run freshness is fresh.
  - Remaining posture: target runs are still degraded for Codex native attach, which is the expected bounded host-capability defer.
- `python scripts\select-next-work.py`
  - Result: `next_action=wait_for_host_capability_recovery`, `evidence_blocker=host_capability_degraded_bounded_defer`, `ltp_decision=defer_all`.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\build-runtime.ps1`
  - Result: pass; `OK python-bytecode`, `OK python-import`.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\verify-repo.ps1 -Check Runtime`
  - Result: pass after live gateway closeout; 118 runtime/service test files completed with `failures=0`.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\verify-repo.ps1 -Check Contract`
  - Result: pass after live gateway closeout; schema, dependency, transition, target governance, shell-risk, pre-change-review, and functional-effectiveness checks passed.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\doctor-runtime.ps1`
  - Result: pass after live gateway closeout with existing `WARN codex-capability-degraded`; no failure exit.
- `git diff --check`
  - Result: pass; only existing CRLF normalization warnings were printed.

## 2026-05-16 Follow-Current Single-Account Update
- Rule IDs: `LocalCodexCockpit`, `CCHS-005B`, `CCHS-005E`, `CCHS-005G`.
- Decision: supersede the former Cockpit multi-account pool lane. The production lane is now Cockpit manual switch -> Cockpit local access `accountIds=[current_account_id]` -> LiteLLM stable upstream -> Codex stable profile.
- Cockpit source change: `D:\CODE\external\cockpit-tools` adds persisted `followCurrentAccount`, a Tauri command to toggle it, UI support in the local access modal, and switch-time sync from `switch_codex_account` to `save_local_access_accounts([account_id], restrictFreeAccounts)`.
- Safety boundary: `CODEX_LOCAL_ACCESS_BIND_HOST` remains unchanged in Cockpit source. The user-requested `0.0.0.0` behavior was not modified.
- Script change: `PrepareCockpitUpstream` now refuses API-key accounts and free accounts by default, then writes exactly one current OAuth account with `followCurrentAccount=true`.
- Local prep command: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Manage-LiteLLMGateway.ps1 -Action PrepareCockpitUpstream`.
  - Result: `enabled=true`, `port=2876`, `account_count=1`, `account_id=codex_310e213ecc0c80158f54625b01579170`, `follow_current_account=true`, `restrict_free_accounts=true`, `firewall_profile_default_block=true`, `restart_required=true`.
  - Backup: `C:\Users\sciman\.antigravity_cockpit\backups\codex_local_access.json.20260516-005002.enable-litellm-upstream.bak`.
- Cockpit source build commands:
  - `npm ci`: pass; installed local dependencies. `npm audit` reports 1 moderate and 3 high vulnerabilities from the dependency tree; not fixed in this change to avoid unrelated dependency churn.
  - `npm run typecheck`: pass.
  - `npm run build`: pass.
  - `winget install --id Rustlang.Rustup -e --accept-package-agreements --accept-source-agreements --silent`: pass; installed Rustup/Rust toolchain for Tauri verification.
  - `cargo check` from `D:\CODE\external\cockpit-tools\src-tauri`: pass with existing unused-code warnings.
  - `npm run tauri -- build`: Rust release binary built at `D:\CODE\external\cockpit-tools\target\release\cockpit-tools.exe`; WiX installer bundling failed with `protocol: http response missing version` while downloading/verifying the WiX package, so MSI packaging is `platform_na` for this run.
- Cockpit Tools process restart:
  - Stopped installed Cockpit Tools PID `24036`.
  - Started custom build from `D:\CODE\external\cockpit-tools\target\release\cockpit-tools.exe`; final verified custom build PID is `32224`.
  - Boundary: Codex App and `codex` processes were not restarted, stopped, killed, or auto-launched.
- Current Cockpit upstream status:
  - `CockpitStatus`: `enabled=true`, `account_count=1`, `follow_current_account=true`, `restrict_free_accounts=true`, `has_api_key=true`, `listener_addresses=["0.0.0.0","127.0.0.1"]`, `firewall_profile_default_block=true`, `safe_for_upstream=true`.
  - Compatibility judgment: listener includes `0.0.0.0` by design and user request; Windows inbound default block constrains LAN ingress for this local upstream lane.
- LiteLLM status:
  - `Stop`/`Start`: restarted only the managed LiteLLM process so it loads the refreshed config.
  - `Smoke`: `/v1/models` through `http://127.0.0.1:4000` returns `cockpit-current` with `secret_redacted=true`.
- Live completion status:
  - `POST http://127.0.0.1:4000/v1/chat/completions` with upstream `gpt-5.4`: reached Cockpit through LiteLLM but returned `429`; Cockpit reported model `gpt-5.4` accounts are cooling down, retry after about `13907` seconds.
  - Temporary retry with upstream `gpt-5.4-mini`: reached Cockpit through LiteLLM but returned `429`; Cockpit reported retry after about `13862` seconds.
  - Interpretation: front-door, LiteLLM, Cockpit API service, and account selection are connected; completion is blocked by the current single account's model cooldown, not by proxy wiring.
- Focused repo tests:
  - `python -m unittest tests.runtime.test_shell_risk_contract tests.runtime.test_codex_cockpit_policy_contract tests.runtime.test_litellm_gateway_contract -v`: 14 tests passed.
  - `git diff --check`: pass; only existing CRLF normalization warnings were printed.
- Hard gate closeout:
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\build-runtime.ps1`: pass.
  - First `Runtime` gate attempt: 117/118 test files passed; `tests.runtime.test_runtime_evolution` timed out at the runner's 180 second per-file timeout.
  - `python -m unittest tests.runtime.test_runtime_evolution -v`: pass, 9 tests passed in about 52 seconds.
  - Second `Runtime` gate attempt: pass, 118 test files completed with `failures=0`.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\verify-repo.ps1 -Check Contract`: pass.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\doctor-runtime.ps1`: pass with existing `WARN codex-capability-degraded`.
- Rollback additions:
  - Stop custom Cockpit Tools PID `32224` if needed and restart the installed Cockpit Tools binary from `C:\Users\sciman\AppData\Local\Cockpit Tools\cockpit-tools.exe`.
  - Restore local access config from `C:\Users\sciman\.antigravity_cockpit\backups\codex_local_access.json.20260516-005002.enable-litellm-upstream.bak` if single-account follow-current mode must be undone.

## N/A And Remaining Live Gates
- `gate_na`: streaming, compact/resume behavior, tool-call traffic, and long-running error propagation.
  - reason: current request was to establish seamless account-switching gateway and end-to-end Codex smoke; long-running traffic coverage requires a separate fixture suite and should not be inferred from a one-shot completion.
  - alternative_verification: live direct Cockpit `/v1/models`, live LiteLLM `/v1/chat/completions`, and live `codex exec` all pass.
  - evidence_link: this file and `docs/runbooks/litellm-cockpit-gateway.md`.
  - expires_at: `2026-05-22`.

## Compatibility And Rollback
- Compatibility: no `[model_providers.openai]` override was introduced; the LiteLLM profile uses custom provider id `litellm_gateway` with `supports_websockets=false`.
- Compatibility: the active Codex App process was not restarted, stopped, killed, or auto-launched.
- Rollback:
  - Stop LiteLLM and remove the managed profile block: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Manage-LiteLLMGateway.ps1 -Action Rollback`.
  - Restore saved Cockpit API provider metadata from `C:\Users\sciman\.codex\config-backups\config.toml.cockpit-saved-provider-20260515-213405.bak` if required.
  - Restore Cockpit launch binding from `C:\Users\sciman\.antigravity_cockpit\backups\codex_instances.json.20260515_213051_cockpit-instance-follow-current.bak` if required.
  - Restore Cockpit local access key file from `C:\Users\sciman\.antigravity_cockpit\backups\codex_local_access.json.20260515_213455.litellm-key-rotate.bak` only if the rotated key causes an operator-visible mismatch.
